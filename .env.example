# Copy this file to .env and fill in values. Never commit the real .env.
# These are the ONLY env var names used across the project — if you need a new one,
# add it here first and tell the team, don't invent a locally-scoped one in your own service.

ARGUS_NEO4J_PASSWORD=changeme_local_dev_only
ARGUS_POSTGRES_PASSWORD=changeme_local_dev_only
