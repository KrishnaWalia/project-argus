# Data Ingestion & External APIs
Owner: Ingestion role. Port 8002. Produces Document objects
(see ../shared/schemas/document.schema.json).

A WORKING STUB ALREADY EXISTS (main.py) — GET /health and a stub POST /ingest/synthetic.
Replace the stub with the real synthetic data generator described in your role prompt.
Synthetic/sample data only — no live scraping of real illegal sites, this build or the demo.
See ../CONVENTIONS.md.
