{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://project-argus/schemas/evidence.schema.json",
  "title": "Evidence",
  "description": "A typed, scored signal linking two documents. Owned by: AI/ML role. Consumed by: Backend role (aggregates into ActorNode), Frontend role (explainability view).",
  "type": "object",
  "required": ["evidence_id", "type", "score", "source_doc_a", "source_doc_b", "details"],
  "properties": {
    "evidence_id": { "type": "string", "format": "uuid" },
    "type": { "type": "string", "enum": ["stylometric", "wallet_reuse", "pgp_reuse", "username_correlation", "temporal"] },
    "score": { "type": "number", "minimum": 0, "maximum": 1, "description": "confidence, not certainty" },
    "source_doc_a": { "type": "string", "format": "uuid", "description": "doc_id" },
    "source_doc_b": { "type": "string", "format": "uuid", "description": "doc_id" },
    "details": {
      "type": "object",
      "description": "type-specific explanation, MUST be human-readable — frontend renders this directly, not raw feature vectors",
      "required": ["summary"],
      "properties": {
        "summary": { "type": "string", "description": "one plain-English sentence, e.g. 'Function-word usage 87% similar; both favor short sentences.'" }
      }
    }
  }
}
